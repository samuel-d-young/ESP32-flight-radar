{
  "name": "esp32-flight-radar",
  "version": "1.0.0",
  "description": "A static site that flashes Flight Radar firmware to ESP32/ESP8266 directly from the browser",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'No build step required - static HTML site'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
